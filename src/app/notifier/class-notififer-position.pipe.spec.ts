import { ClassNotififerPositionPipe } from './class-notififer-position.pipe';

describe('ClassNotififerPositionPipe', () => {
  it('create an instance', () => {
    const pipe = new ClassNotififerPositionPipe();
    expect(pipe).toBeTruthy();
  });
});
